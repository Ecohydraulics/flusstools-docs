# ---------------------------------------------------------------------------
# Read the Docs build requirements for the flusstools documentation.
#
# This repo holds ONLY the documentation (.rst) sources - the flusstools code
# lives in https://github.com/Ecohydraulics/flusstools-pckg and is published to
# PyPI. This file installs just the Sphinx toolchain. flusstools itself is
# installed separately and --no-deps in .readthedocs.yaml (build.jobs.post_install),
# so Read the Docs never has to build GDAL or the rest of the geospatial stack;
# those imports are mocked in docs/conf.py via `autodoc_mock_imports`.
# ---------------------------------------------------------------------------

# Sphinx toolchain (matches the extensions enabled in docs/conf.py)
Sphinx>=7.3
sphinx-book-theme>=1.1
sphinx-copybutton>=0.5
sphinx-togglebutton>=0.3
sphinx-thebe>=0.3
myst-nb>=1.1
ipython>=8.0
